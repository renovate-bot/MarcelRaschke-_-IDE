{
  "appliesTo": [
    {
      "when": {
        "$ref": "/definitions/conditions.json#/isLocalEnvironment"
      },
      "config": "cosmosdb.emulator"
    },
    {
      "when": {
        "$ref": "/definitions/conditions.json#/local"
      },
      "config": "cosmosdb.emulator"
    }
  ]
}
